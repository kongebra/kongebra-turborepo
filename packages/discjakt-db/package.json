{
  "name": "discjakt-db",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate && tsc",
    "prebuild": "npm run generate",
    "predev": "npm run generate"
  },
  "dependencies": {
    "@prisma/client": "^4.6.1"
  },
  "devDependencies": {
    "prisma": "^4.6.1",
    "typescript": "^4.9.3"
  }
}
